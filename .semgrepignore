# -----------------------------------------------------------------------------
# SEMGREP IGNORE FILE
# -----------------------------------------------------------------------------
# Ce fichier configure Semgrep pour ignorer les répertoires et fichiers
# non applicables aux scans SAST de code source (ex: configurations K8s, scripts de test).
# -----------------------------------------------------------------------------

# Exclure les manifestes Kubernetes (déjà scannés et gérés par Checkov)
k8s/

# Exclure les configurations Ansible (déjà scannées et gérées par Checkov)
nsia_iam_central_enforcer/

# Exclure les scripts de tests d'intrusion et tests OWASP (contenant des faux tokens/payloads)
security-tests/

# Exclure les certificats et clés SSL générés pour le POC
kong.key
kong.crt
*.key
*.crt

# Exclure les templates HTML et répertoires de fichiers statiques (évite le bruit sur SRI, CSP, etc.)
**/templates/
**/static/
**/staticfiles/
*.html

# Exclure les répertoires d'infrastructure et d'outils annexes
api-security-fabric/

# Exclure les migrations Django (du code SQL/DDL brut auto-généré y est normal)
**/migrations/

# Exclure les tests (les faux jetons et clés de simulation y sont autorisés)
**/tests.py
**/tests/

# Exclure la documentation
*.md
**/docs/
**/README.md

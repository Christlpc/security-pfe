_format_version: "3.0"
services:
  - name: nsia-backend-service
    url: http://nsia-api-metier:8000
    routes:
      - name: api-v1-banques-get
        paths:
          - /api/v1/banques/
        methods:
          - GET
        strip_path: false
        plugins:
          - name: key-auth
            config:
                key_names:
                  - apikey
            
          - name: acl
            config:
                allow:
                  - admins
      - name: api-v1-banques-post
        paths:
          - /api/v1/banques/
        methods:
          - POST
        strip_path: false
        plugins:
          - name: key-auth
            config:
                key_names:
                  - apikey
            
          - name: acl
            config:
                allow:
                  - admins
      - name: api-v1-banques-id-get
        paths:
          - ~/api/v1/banques/(?<id>[^/]+)/
        methods:
          - GET
        strip_path: false
        plugins:
          - name: key-auth
            config:
                key_names:
                  - apikey
            
          - name: acl
            config:
                allow:
                  - admins
      - name: api-v1-banques-id-put
        paths:
          - ~/api/v1/banques/(?<id>[^/]+)/
        methods:
          - PUT
        strip_path: false
        plugins:
          - name: key-auth
            config:
                key_names:
                  - apikey
            
          - name: acl
            config:
                allow:
                  - admins
      - name: api-v1-banques-id-patch
        paths:
          - ~/api/v1/banques/(?<id>[^/]+)/
        methods:
          - PATCH
        strip_path: false
        plugins:
          - name: key-auth
            config:
                key_names:
                  - apikey
            
          - name: acl
            config:
                allow:
                  - admins
      - name: api-v1-banques-id-delete
        paths:
          - ~/api/v1/banques/(?<id>[^/]+)/
        methods:
          - DELETE
        strip_path: false
        plugins:
          - name: key-auth
            config:
                key_names:
                  - apikey
            
          - name: acl
            config:
                allow:
                  - admins
      - name: api-v1-banques-id-utilisateurs-get
        paths:
          - ~/api/v1/banques/(?<id>[^/]+)/utilisateurs/
        methods:
          - GET
        strip_path: false
        plugins:
          - name: key-auth
            config:
                key_names:
                  - apikey
            
          - name: acl
            config:
                allow:
                  - admins
      - name: api-v1-simulations-elikia-post
        paths:
          - /api/v1/simulations/elikia/
        methods:
          - POST
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-emprunteur-post
        paths:
          - /api/v1/simulations/emprunteur/
        methods:
          - POST
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-etudes-post
        paths:
          - /api/v1/simulations/etudes/
        methods:
          - POST
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-historique-get
        paths:
          - /api/v1/simulations/historique/
        methods:
          - GET
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-historique-post
        paths:
          - /api/v1/simulations/historique/
        methods:
          - POST
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-historique-id-get
        paths:
          - ~/api/v1/simulations/historique/(?<id>[^/]+)/
        methods:
          - GET
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-historique-id-put
        paths:
          - ~/api/v1/simulations/historique/(?<id>[^/]+)/
        methods:
          - PUT
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-historique-id-patch
        paths:
          - ~/api/v1/simulations/historique/(?<id>[^/]+)/
        methods:
          - PATCH
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-historique-id-delete
        paths:
          - ~/api/v1/simulations/historique/(?<id>[^/]+)/
        methods:
          - DELETE
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-historique-id-souscrire-post
        paths:
          - ~/api/v1/simulations/historique/(?<id>[^/]+)/souscrire/
        methods:
          - POST
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-historique-id-valider-post
        paths:
          - ~/api/v1/simulations/historique/(?<id>[^/]+)/valider/
        methods:
          - POST
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-mobateli-post
        paths:
          - /api/v1/simulations/mobateli/
        methods:
          - POST
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-questionnaires-medicaux-get
        paths:
          - /api/v1/simulations/questionnaires-medicaux/
        methods:
          - GET
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-questionnaires-medicaux-post
        paths:
          - /api/v1/simulations/questionnaires-medicaux/
        methods:
          - POST
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-questionnaires-medicaux-id-get
        paths:
          - ~/api/v1/simulations/questionnaires-medicaux/(?<id>[^/]+)/
        methods:
          - GET
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-questionnaires-medicaux-id-put
        paths:
          - ~/api/v1/simulations/questionnaires-medicaux/(?<id>[^/]+)/
        methods:
          - PUT
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-questionnaires-medicaux-id-patch
        paths:
          - ~/api/v1/simulations/questionnaires-medicaux/(?<id>[^/]+)/
        methods:
          - PATCH
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-questionnaires-medicaux-id-delete
        paths:
          - ~/api/v1/simulations/questionnaires-medicaux/(?<id>[^/]+)/
        methods:
          - DELETE
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-questionnaires-medicaux-id-appliquer-a-simulation-post
        paths:
          - ~/api/v1/simulations/questionnaires-medicaux/(?<id>[^/]+)/appliquer-a-simulation/
        methods:
          - POST
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-questionnaires-medicaux-id-recalculer-surprime-post
        paths:
          - ~/api/v1/simulations/questionnaires-medicaux/(?<id>[^/]+)/recalculer-surprime/
        methods:
          - POST
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-questionnaires-medicaux-bareme-get
        paths:
          - /api/v1/simulations/questionnaires-medicaux/bareme/
        methods:
          - GET
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-retraite-post
        paths:
          - /api/v1/simulations/retraite/
        methods:
          - POST
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-simulations-simulation_id-bia-info-get
        paths:
          - ~/api/v1/simulations/simulations/(?<simulation_id>[^/]+)/bia-info/
        methods:
          - GET
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-simulations-simulation_id-export-bia-get
        paths:
          - ~/api/v1/simulations/simulations/(?<simulation_id>[^/]+)/export-bia/
        methods:
          - GET
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-simulations-simulation_id-preview-bia-get
        paths:
          - ~/api/v1/simulations/simulations/(?<simulation_id>[^/]+)/preview-bia/
        methods:
          - GET
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-souscriptions-get
        paths:
          - /api/v1/simulations/souscriptions/
        methods:
          - GET
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-souscriptions-post
        paths:
          - /api/v1/simulations/souscriptions/
        methods:
          - POST
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-souscriptions-id-get
        paths:
          - ~/api/v1/simulations/souscriptions/(?<id>[^/]+)/
        methods:
          - GET
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-souscriptions-id-put
        paths:
          - ~/api/v1/simulations/souscriptions/(?<id>[^/]+)/
        methods:
          - PUT
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-souscriptions-id-patch
        paths:
          - ~/api/v1/simulations/souscriptions/(?<id>[^/]+)/
        methods:
          - PATCH
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-souscriptions-id-delete
        paths:
          - ~/api/v1/simulations/souscriptions/(?<id>[^/]+)/
        methods:
          - DELETE
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-souscriptions-id-rejeter-post
        paths:
          - ~/api/v1/simulations/souscriptions/(?<id>[^/]+)/rejeter/
        methods:
          - POST
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-simulations-souscriptions-id-valider-post
        paths:
          - ~/api/v1/simulations/souscriptions/(?<id>[^/]+)/valider/
        methods:
          - POST
        strip_path: false
        plugins:
          - name: jwt
            config:
                claims_to_verify:
                  - exp
            
          - name: rate-limiting
            config:
                minute: 5
                policy: local
            
          - name: acl
            config:
                allow:
                  - partners
            
          - name: request-size-limiting
            config:
                allowed_payload_size: 1
      - name: api-v1-utilisateurs-get
        paths:
          - /api/v1/utilisateurs/
        methods:
          - GET
        strip_path: false
        plugins:
          - name: key-auth
            config:
                key_names:
                  - apikey
            
          - name: acl
            config:
                allow:
                  - admins
      - name: api-v1-utilisateurs-post
        paths:
          - /api/v1/utilisateurs/
        methods:
          - POST
        strip_path: false
        plugins:
          - name: key-auth
            config:
                key_names:
                  - apikey
            
          - name: acl
            config:
                allow:
                  - admins
      - name: api-v1-utilisateurs-id-get
        paths:
          - ~/api/v1/utilisateurs/(?<id>[^/]+)/
        methods:
          - GET
        strip_path: false
        plugins:
          - name: key-auth
            config:
                key_names:
                  - apikey
            
          - name: acl
            config:
                allow:
                  - admins
      - name: api-v1-utilisateurs-id-put
        paths:
          - ~/api/v1/utilisateurs/(?<id>[^/]+)/
        methods:
          - PUT
        strip_path: false
        plugins:
          - name: key-auth
            config:
                key_names:
                  - apikey
            
          - name: acl
            config:
                allow:
                  - admins
      - name: api-v1-utilisateurs-id-patch
        paths:
          - ~/api/v1/utilisateurs/(?<id>[^/]+)/
        methods:
          - PATCH
        strip_path: false
        plugins:
          - name: key-auth
            config:
                key_names:
                  - apikey
            
          - name: acl
            config:
                allow:
                  - admins
      - name: api-v1-utilisateurs-id-delete
        paths:
          - ~/api/v1/utilisateurs/(?<id>[^/]+)/
        methods:
          - DELETE
        strip_path: false
        plugins:
          - name: key-auth
            config:
                key_names:
                  - apikey
            
          - name: acl
            config:
                allow:
                  - admins
      - name: api-v1-utilisateurs-id-reset_password-post
        paths:
          - ~/api/v1/utilisateurs/(?<id>[^/]+)/reset_password/
        methods:
          - POST
        strip_path: false
        plugins:
          - name: key-auth
            config:
                key_names:
                  - apikey
            
          - name: acl
            config:
                allow:
                  - admins
      - name: api-v1-utilisateurs-id-toggle_status-post
        paths:
          - ~/api/v1/utilisateurs/(?<id>[^/]+)/toggle_status/
        methods:
          - POST
        strip_path: false
        plugins:
          - name: key-auth
            config:
                key_names:
                  - apikey
            
          - name: acl
            config:
                allow:
                  - admins
  - name: nsia-metier-service
    url: http://nsia-api-metier:8000
    routes:
      - name: nsia-metier-route
        paths:
          - /api
        strip_path: false
        plugins:
          - name: jwt
  - name: keycloak-service
    url: http://keycloak-iam:8080
    routes:
      - name: keycloak-ui-route
        paths:
          - /
        strip_path: false
certificates:
  - cert: |
      -----BEGIN CERTIFICATE-----
      
      -----END CERTIFICATE-----
    key: |
      -----BEGIN PRIVATE KEY-----
      
      37k5S/Fvdyf/m64fFHFycgy0
      -----END PRIVATE KEY-----
    snis:
      - name: localhost
      - name: nsia-pfe-api

consumers:
  - username: test-partner
    acls:
      - group: partners
    jwt_secrets:
      - key: test-iss
        algorithm: HS256
        secret: secret_partage_poc_nsia
  
  # Configuration pour valider les tokens émis par Keycloak (Multi-tenant)
  - username: keycloak-master
    jwt_secrets:
      - key: http://localhost:8080/realms/master
        algorithm: RS256
        secret: "ignored_for_rs256"
        rsa_public_key: "-----BEGIN PUBLIC KEY-----\\nTQIDAQAB\n-----END PUBLIC KEY-----"

  - username: keycloak-bank-lcb
    jwt_secrets:
      - key: http://localhost:8080/realms/BANK_LCB
        algorithm: RS256
        secret: "ignored_for_rs256"
        rsa_public_key: "-----BEGIN PUBLIC KEY-----\\nTQIDAQAB\n-----END PUBLIC KEY-----"
